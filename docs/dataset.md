# Dataset Notes

Document the dataset source, preprocessing steps, file naming convention, sensor bands, and any access restrictions here before sharing the repository publicly.

Large `.h5` data files should not be committed directly to git.
